import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  body: {
    color: "white",
    marginTop: "12%",
    width: "100%",
    padding: "0 100px",
  },
  heading: {
    fontWeight: "bold",
    marginBottom: "30px",
  },
  prompt: {
    textAlign: "center",
    marginTop: "11%"
  },
  icon: {
      color: "white"
  }
});

export default useStyles;
