import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
    text: {
        color: "white",
        marginTop: "10%",
        width: "80%",
        padding: "0 100px"
    },
    heading: {
        fontWeight: "bold",
        marginBottom: "30px",
    }
});

export default useStyles;